spring:
  config:
    import: optional:file:.env[.properties],optional:file:system/.env[.properties]
  datasource:
    url: jdbc:postgresql://${HOST:postgres:5432}/${POSTGRES_DB}?currentSchema=${POSTGRES_SCHEMA}

server:
  port: 8081

